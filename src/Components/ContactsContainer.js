import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ContactsContainer = () => {
    return (
        <View>
            <Text>ContactsContainer</Text>
        </View>
    );
};

export default ContactsContainer;

const styles = StyleSheet.create({});
